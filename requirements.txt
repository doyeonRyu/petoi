# Core dependencies
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-community>=0.2.0
langchain-core>=0.2.0
openai>=1.3.0

# Environment management
python-dotenv>=1.0.0

# Speech recognition & TTS
SpeechRecognition>=3.10.0
pydub>=0.25.1
gTTS>=2.3.2
playsound>=1.3.0

# JSON & OS utilities
jsonschema>=4.19.0

# Petoi robot control
PetoiRobot>=1.0.0  # (또는 직접 만든 모듈이라면 로컬 설치용)

# Optional: Whisper(STT)나 기타 음성 모델 사용할 경우
whisper>=1.0  # (선택사항, 직접 모델 사용하는 경우에만)

# If using file-based memory
tiktoken>=0.5.2
